apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - nanogateway.yaml

patches:
  - path: ingresscontroller-wildcard-routes.yaml
    target:
      kind: IngressController
      name: default
      namespace: openshift-ingress-operator